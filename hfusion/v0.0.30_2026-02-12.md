# hfusion v0.0.30（2026-02-12）

## 变更概览

- 添加启动栈脚本，统一管理前后端启动
- 优化开发环境启动体验
- 更新项目文档和 README
- 添加依赖检查哈希文件

## 详情

### Added
- 新增 `start-stack.ps1` 启动栈脚本，支持同时启动前后端
- 新增 `start-prod.ps1` 生产环境启动脚本
- 新增 `.requirements.hash` 和 `.deps.hash` 依赖检查文件

### Changed
- 优化 `start-dev.ps1` 开发启动脚本
- 优化 `start-backend.ps1` 后端启动逻辑
- 优化 `start-frontend.ps1` 前端启动逻辑
- 优化 `check-env.ps1` 环境检查脚本
- 更新 `start-dev.bat` 和 `start-prod.bat` 批处理脚本

### Documentation
- 更新 README.md 项目文档

## 备注

- 此版本主要优化开发工作流和启动脚本
- 新增依赖哈希检查机制，避免重复安装
- 启动栈脚本提供更统一的开发体验

## Technical Details

- Commit: `b6dec02` - chore: 优化启动脚本并更新文档 (2026-02-12)
