# hfusion v0.0.16（2026-01-29）

## 变更概览

- 移除所有 UI 组件圆角，统一直角设计风格
- 更新基础组件（button、card、input、skeleton）移除默认圆角
- 更新导航和下拉菜单组件移除圆角样式

## 详情

### Changed
- 移除 button 组件默认 `rounded-md` 圆角样式
- 移除 card 组件 `rounded-xl` 圆角样式
- 移除 input 组件 `rounded-md` 圆角样式
- 移除 skeleton 组件 `rounded-md` 圆角样式
- 移除 dropdown-menu 组件多处圆角样式（rounded-md、rounded-sm）
- 移除 navigation-menu 组件多处圆角样式（rounded-md、rounded-sm）
- 移除 typewriter-effect 光标圆角样式

### UX
- 统一整体 UI 为直角边框设计风格
- 更新 workspace 模块组件（MainPrompt、ProjectGrid、Sidebar）移除圆角
- 更新 canvas 模块组件（CanvasChatPanel、CanvasToolbar）移除圆角
- 更新 landing 模块组件（Hero）移除圆角
- 更新 lab 模块组件（LabHero）移除圆角
- 更新 gallery 模块组件（GalleryHero）移除圆角
- 更新 crafts 模块组件（CraftCard、CraftsCategoryGrid）移除圆角
- 更新 shared 组件（MainNavbar、PageLoadingSkeleton、UnderConstruction、mode-toggle）移除圆角

## 备注

本次更新将整体 UI 风格从圆角设计改为直角设计，涉及 21 个文件的样式调整。

## Technical Details

- Commit: `77fdc35` - style(ui): 移除所有组件圆角，统一直角设计风格 (2026-01-29)
