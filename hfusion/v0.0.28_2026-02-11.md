# hfusion v0.0.28（2026-02-11）

## 变更概览

- 新增用户会话管理系统，支持 refresh token 轮换
- 实现设备管理和会话列表功能
- 增强 OAuth 服务和认证服务
- 前端账户设置页面支持会话管理

## 详情

### Added

- 新增 `UserSession` 模型用于管理用户登录会话
- 新增 `user_session_repo.py` 用户会话仓库
- 新增 `session.py` 会话相关 Schema
- 新增数据库迁移文件 `a1f4c2d8e901_add_user_sessions.py`
- 实现设备管理和会话列表 API
- 前端账户设置页面新增会话管理功能

### Changed

- 重构 `auth_service.py`，增强认证服务逻辑
- 优化 `oauth_service.py`，改进 OAuth 流程
- 优化 Redis 缓存策略
- 更新 `api/deps.py` 依赖注入
- 增强 `security.py` 安全模块
- 更新前端 `auth.ts` 和 `client.ts` API 客户端

### Technical

- 支持 refresh token 轮换机制
- 会话记录包含 User-Agent 和 IP 地址
- 新增会话过期时间和最后使用时间追踪

## 备注

- 此版本引入了完整的用户会话管理系统
- 支持用户查看和管理自己的登录设备
- 增强了账户安全性

## Technical Details

- Commit: `f3695db` - feat(auth): add user session management and device management (2026-02-11 18:01)
