# AutoTags v0.0.41（2025-10-29）

## 变更概览

- 新增基于近期提交创建标签的模式

## 详情

### 新增
- 添加 `-l`/`--latest` 选项，用于在创建标签时分析近期提交
- 此模式下会跳过 `add`/`commit`/`push` 流程，直接获取最近 10 条提交的详细信息，并通过 AI 生成有意义的标签注释
- 该功能可与 `-cr` 结合使用，以创建 Release

## Technical Details

- Commit: `af2c850` - 新增基于近期提交创建标签的模式 (2025-10-29 01:09)
