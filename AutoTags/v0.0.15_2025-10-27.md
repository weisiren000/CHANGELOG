# AutoTags v0.0.15（2025-10-27）

## 变更概览

- 支持创建 GitHub Release 并增强 CLI 与依赖检查

## 详情

### 新增
- 新增 --create_release (-cr) 选项，用于在打标签后自动创建 GitHub Release
- 优先使用 gh CLI，若不可用则回退到 GitHub API（需 GITHUB_TOKEN）
- 新增 deps 模块：
  - 检查 git 可用性
  - 提供 gh 安装提示

### 重构
- 重构 CLI 参数处理逻辑，返回 CliOptions 结构体

### 文档
- 更新帮助信息，说明新选项和 GITHUB_TOKEN 环境变量

## Technical Details

- Commit: `219a4d1` - feat: 支持创建 GitHub Release 并增强 CLI 与依赖检查 (2025-10-27 21:04)
