# AutoTags v0.0.23（2025-10-27）

## 变更概览

- 嵌入文档并支持运行时读取

## 详情

### 新增
- 引入 `include_dir` crate 将 `docs/` 目录嵌入二进制
- 实现 `collect_embedded_docs` 函数解析嵌入文档的编号与内容
- 更新 `--docs` 选项逻辑，优先使用嵌入文档而非本地文件
- 微调帮助信息中 `--docs` 参数的占位符为 `[num]`

## Technical Details

- Commit: `c2e4f40` - feat(cli): 嵌入文档并支持运行时读取 (2025-10-27 23:11)
