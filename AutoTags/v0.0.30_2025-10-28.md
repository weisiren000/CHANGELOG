# AutoTags v0.0.30（2025-10-28）

## 变更概览

- 支持创建自定义名称的标签

## 详情

### 新增
- 在 `CliOptions` 中新增 `tag_name` 字段，用于存储用户指定的标签名
- `handle_args` 函数现在解析 `-ct` 命令后的 `-s` 或 `--set` 选项，并读取其后的参数作为标签名
- 主程序逻辑相应调整，优先使用自定义标签名，若未提供则回退到自动计算下一个版本号

### 文档
- 更新了帮助文档，增加了自定义标签的用法示例

## Technical Details

- Commit: `3a8506f` - feat(cli): 支持创建自定义名称的标签 (2025-10-28 21:02)
