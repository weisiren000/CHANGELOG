# AutoTags v0.0.16（2025-10-27）

## 变更概览

- 依赖 gh CLI 创建 Release，移除 GITHUB_TOKEN 回退逻辑

## 详情

### 重构
- 为确保 GitHub Release 创建流程的可靠性，现在强制要求用户预先安装并登录 gh CLI
- 更新帮助信息，明确 -cr 选项依赖 gh 且需提前登录
- 在主流程中增加 gh 可用性及认证状态的预检，未满足条件时提前退出
- 移除原有通过 GITHUB_TOKEN 调用 GitHub API 的备用逻辑
- 简化 ensure_gh_auth 实现，统一跨平台 token 登录方式，并移除已废弃的 --check 参数

## Technical Details

- Commit: `db271a7` - refactor(release): 依赖 gh CLI 创建 Release，移除 GITHUB_TOKEN 回退逻辑 (2025-10-27 21:13)
