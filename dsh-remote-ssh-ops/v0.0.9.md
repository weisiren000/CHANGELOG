# dsh-remote-ssh-ops v0.0.9（2026-08-17）

## 改进
- 项目名称调整为 `dsh-remote-ssh-ops`，更明确地标识 SSH 远程运维能力。
- 统一插件安装、卸载和远端 `remote-hostd` 使用的新项目名称。
- 统一本机远程运维数据和远端 hostd 数据的默认存储目录，避免不同组件使用不同目录。

## 问题修复
- 修复改名后客户端 bundle 仍使用旧插件标识的问题。
